{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build walrus-lsp",
      "type": "shell",
      "command": "cargo",
      "args": [
        "build",
        "--bin",
        "walrus-lsp"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$rustc"
      ],
      "group": "build"
    }
  ]
}
