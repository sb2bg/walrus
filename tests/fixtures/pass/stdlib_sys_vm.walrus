import "std/sys";
import "std/core" as core;

let missing = sys.env_get("WALRUS_TEST_MISSING_ENV_42F7");
println(missing == void);

println(core.type(sys.args()));
println(core.len(sys.args()));

let cwd = sys.cwd();
println(core.type(cwd));
println(core.len(cwd) > 0);
