let suffix = "!";

struct Greeter {
    fn init : self, name {
        self.name = name;
    }

    fn greet : self {
        return self.name + suffix;
    }
}

fn set_suffix : next {
    suffix = next;
}

fn greet_name : name {
    let greeter = Greeter(name);
    return greeter.greet();
}
