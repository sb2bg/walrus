fn factorial : n {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

println(factorial(5));
println(factorial(6));

fn countdown : n {
    if n <= 0 {
        println("Done!");
        return 0;
    }
    println(n);
    countdown(n - 1);
}

countdown(5);
