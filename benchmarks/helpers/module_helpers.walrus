let answer = 42;
let name = "module_helpers";

fn mix : a, b {
    return a * 3 + b * 2 - (a % 5);
}

fn twist : x {
    return mix(x, x % 7) + answer;
}

fn pair_sum : values {
    let total = 0;
    for value in values {
        total = total + twist(value);
    }
    return total;
}
